# Android

This folder contains Android platform files for the React Native project.

Common tasks

- Open Android project in Android Studio: `android/`
- Build from command line:

```bash
cd android && ./gradlew assembleDebug
```

Local files such as `local.properties` and keystores are ignored by `.gitignore`.
