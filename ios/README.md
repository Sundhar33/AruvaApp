# iOS

This folder contains iOS platform files used when building the app natively.

Common tasks

- Open the workspace in Xcode: open `ios/*.xcworkspace`
- Install CocoaPods (if needed):

```bash
cd ios && pod install
```

Xcode build artifacts and CocoaPods `Pods/` are ignored by `.gitignore`.
